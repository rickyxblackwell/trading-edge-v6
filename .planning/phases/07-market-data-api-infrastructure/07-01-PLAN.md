---
phase: 07-market-data-api-infrastructure
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .env.local
  - .planning/phases/07-market-data-api-infrastructure/07-01-WAVE0-LOG.md
autonomous: false
requirements:
  - MDATA-04
  - MDATA-05
tags:
  - env-setup
  - prerequisite
  - polygon
  - supabase
user_setup:
  - service: supabase
    why: "Server-side memory writes require service role key (D-06)"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API → Service Role Key (secret)"
  - service: fred
    why: "Macro economic series tools (D-08)"
    env_vars:
      - name: FRED_API_KEY
        source: "https://fredaccount.stlouisfed.org/apikeys (free, instant)"
  - service: polygon
    why: "CME futures tier verification — tool palette depends on plan access (D-09, RESEARCH Q1)"
    dashboard_config:
      - task: "Confirm Polygon plan grants CME futures REST access (test GET /futures/v1/contracts?ticker=ES)"
        location: "https://polygon.io/dashboard"

must_haves:
  truths:
    - "SUPABASE_SERVICE_ROLE_KEY is present in .env.local and is NOT prefixed with NEXT_PUBLIC_"
    - "FRED_API_KEY is present in .env.local and is NOT prefixed with NEXT_PUBLIC_"
    - "Developer has run a curl test against Polygon futures endpoint with their key and recorded the result (200 / 403 / 401)"
    - "Developer has run a curl test against Alpha Vantage MCP URL with a valid AV key and recorded reachability"
    - "Wave 0 log file documents tier outcome so plan 07-03 knows whether Polygon tool returns real data or graceful fallback"
  artifacts:
    - path: ".env.local"
      provides: "Server-only secrets for FRED + Supabase service role"
      contains: "SUPABASE_SERVICE_ROLE_KEY="
    - path: ".env.local"
      provides: "FRED API key"
      contains: "FRED_API_KEY="
    - path: ".planning/phases/07-market-data-api-infrastructure/07-01-WAVE0-LOG.md"
      provides: "Documented Polygon tier result + AV MCP reachability for downstream plans"
  key_links:
    - from: ".env.local"
      to: "process.env.SUPABASE_SERVICE_ROLE_KEY"
      via: "Next.js server-only env var resolution"
      pattern: "SUPABASE_SERVICE_ROLE_KEY"
    - from: ".env.local"
      to: "process.env.FRED_API_KEY"
      via: "Next.js server-only env var resolution"
      pattern: "FRED_API_KEY"
---

<objective>
Wave 0 prerequisite gate. Before any code in Phase 7 is written, the developer must (a) provision two new server-only env vars (`SUPABASE_SERVICE_ROLE_KEY`, `FRED_API_KEY`) per D-06/D-08, and (b) verify Polygon.io futures tier access per RESEARCH.md Open Question #1. The result of the Polygon verification determines whether plan 07-03 builds a fully-functional Polygon tool or a graceful-degradation-only stub.

This plan blocks Wave 1+. Without these env vars, the agentic loop cannot write memory (route returns 500) and FRED tool calls fail.

Purpose: De-risk the phase. Polygon tier is the highest-confidence-gap in research (LOW confidence). Env vars are a hard prerequisite — no code path can succeed without them.
Output: `.env.local` populated, Wave 0 log file recording Polygon tier + AV MCP reachability so the executor for 07-03 knows which Polygon code path to ship.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-market-data-api-infrastructure/07-CONTEXT.md
@.planning/phases/07-market-data-api-infrastructure/07-RESEARCH.md

<interfaces>
<!-- No code interfaces — this is an env + verification plan. -->

Required env var names (exact, server-side, no NEXT_PUBLIC_ prefix):
- SUPABASE_SERVICE_ROLE_KEY  (Supabase Dashboard → API → service_role secret)
- FRED_API_KEY               (https://fredaccount.stlouisfed.org/apikeys)

Polygon verification curl (use any registered Polygon API key the user controls):
  curl -sS "https://api.polygon.io/v3/reference/tickers?market=fx&limit=1&apiKey=$POLYGON_API_KEY"     # sanity ping
  curl -sS "https://api.polygon.io/futures/v1/contracts?product_code=ES&apiKey=$POLYGON_API_KEY"      # tier probe

AV MCP reachability curl:
  curl -sS -i "https://mcp.alphavantage.co/mcp?apikey=$AV_API_KEY" --max-time 10
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision SUPABASE_SERVICE_ROLE_KEY and FRED_API_KEY in .env.local</name>
  <files>.env.local</files>
  <read_first>
    - .env.local (current contents — confirm which vars already exist)
    - .planning/phases/07-market-data-api-infrastructure/07-RESEARCH.md (Pattern 5 — service role usage; Pitfall 3 — wrong client; Anti-Pattern: leaking via NEXT_PUBLIC_ prefix)
    - .planning/phases/07-market-data-api-infrastructure/07-CONTEXT.md (D-06 — server-side memory writes)
  </read_first>
  <what-built>Nothing yet — this checkpoint blocks Wave 1 until human action completes.</what-built>
  <how-to-verify>
    1. Open `.env.local` in the project root.
    2. Confirm these two lines exist (values omitted in plan — user supplies):
         SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...   (NOT prefixed with NEXT_PUBLIC_)
         FRED_API_KEY=abcd1234...
    3. Source for SUPABASE_SERVICE_ROLE_KEY: Supabase Dashboard → Project Settings → API → "service_role" key (the secret one, NOT the anon key). NEVER paste this key into any client-side file or code committed to git.
    4. Source for FRED_API_KEY: register at https://fredaccount.stlouisfed.org/apikeys (free, instant). One key suffices for the entire project (D-08 — shared server-side key).
    5. Run from the project root:
         grep -c '^SUPABASE_SERVICE_ROLE_KEY=' .env.local
         grep -c '^FRED_API_KEY=' .env.local
       Both must return `1`.
    6. Confirm neither var is prefixed with `NEXT_PUBLIC_` (would leak to client bundle):
         grep -c '^NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY=' .env.local   # MUST return 0
         grep -c '^NEXT_PUBLIC_FRED_API_KEY=' .env.local                # MUST return 0
    7. Confirm `.env.local` is git-ignored (run `git check-ignore .env.local` — exit code 0 means ignored).
  </how-to-verify>
  <acceptance_criteria>
    - `grep -c '^SUPABASE_SERVICE_ROLE_KEY=' .env.local` returns `1`
    - `grep -c '^FRED_API_KEY=' .env.local` returns `1`
    - `grep -c '^NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY=' .env.local` returns `0`
    - `grep -c '^NEXT_PUBLIC_FRED_API_KEY=' .env.local` returns `0`
    - `git check-ignore .env.local` exits 0 (file is git-ignored)
  </acceptance_criteria>
  <resume-signal>Type "env-vars-provisioned" once both keys are in .env.local and the four grep checks pass.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Verify Polygon CME futures tier access + AV MCP reachability</name>
  <files>.planning/phases/07-market-data-api-infrastructure/07-01-WAVE0-LOG.md</files>
  <read_first>
    - .planning/phases/07-market-data-api-infrastructure/07-RESEARCH.md (Open Question 1 — Polygon futures plan; Pitfall 4 — graceful 403; Assumption A1)
    - .planning/phases/07-market-data-api-infrastructure/07-CONTEXT.md (D-09 — Polygon endpoints scope)
  </read_first>
  <what-built>A short log file at `.planning/phases/07-market-data-api-infrastructure/07-01-WAVE0-LOG.md` recording: (a) Polygon futures tier outcome, (b) AV MCP HTTP reachability. Plan 07-03 reads this file to decide whether `fetchPolygonFutures` returns real data or always-graceful-fallback.</what-built>
  <how-to-verify>
    1. Obtain the Polygon API key currently configured in `user_metadata.polygon_api_key` (or use the developer's own Polygon key) — this is for verification only; do not commit the key.
    2. Run the futures tier probe (terminal, project root, replace placeholder):
         curl -sS -o /tmp/polygon-probe.json -w "HTTP %{http_code}\n" \
           "https://api.polygon.io/futures/v1/contracts?product_code=ES&apiKey=YOUR_KEY"
       Record HTTP status code:
         - 200 → futures API available; record `polygon_tier: full`
         - 403 → futures requires upgrade; record `polygon_tier: forbidden`
         - 401 → key invalid; do NOT proceed — re-issue key first
         - 404 → endpoint moved; record `polygon_tier: endpoint_unknown` and capture response body
    3. Run AV MCP reachability probe (use any registered AV key — free signup at alphavantage.co):
         curl -sS -i --max-time 10 "https://mcp.alphavantage.co/mcp?apikey=YOUR_AV_KEY" | head -20
       Record HTTP status code from first line. 200/SSE handshake = reachable. Network error / timeout = note for retry.
    4. Create the log file with EXACT structure below (use the Write tool):

       File path: `.planning/phases/07-market-data-api-infrastructure/07-01-WAVE0-LOG.md`

       Contents:
       ```
       # Phase 07 Wave 0 — Verification Log

       Date: <YYYY-MM-DD>

       ## Polygon Futures Tier
       - Probe URL: https://api.polygon.io/futures/v1/contracts?product_code=ES
       - HTTP status: <200 | 403 | 404 | 401>
       - Polygon tier: <full | forbidden | endpoint_unknown>
       - Plan 07-03 instruction: <"Build full fetchPolygonFutures with bars + OI" | "Build fetchPolygonFutures stub that returns '[Polygon futures requires plan upgrade]' on every call without making the network request" | "Skip Polygon tool; remove from tool palette">

       ## Alpha Vantage MCP Reachability
       - URL: https://mcp.alphavantage.co/mcp
       - HTTP status: <200 | other>
       - Reachable: <yes | no>

       ## Env vars confirmed (Task 1)
       - SUPABASE_SERVICE_ROLE_KEY: present
       - FRED_API_KEY: present
       ```

    5. Run from project root:
         test -f .planning/phases/07-market-data-api-infrastructure/07-01-WAVE0-LOG.md && echo "EXISTS"
       Must print `EXISTS`.
    6. Run:
         grep -c "Polygon tier:" .planning/phases/07-market-data-api-infrastructure/07-01-WAVE0-LOG.md
         grep -c "Reachable:" .planning/phases/07-market-data-api-infrastructure/07-01-WAVE0-LOG.md
       Each must return `1`.
  </how-to-verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/07-market-data-api-infrastructure/07-01-WAVE0-LOG.md`
    - `grep -c "Polygon tier:" .planning/phases/07-market-data-api-infrastructure/07-01-WAVE0-LOG.md` returns `1`
    - `grep -c "Reachable:" .planning/phases/07-market-data-api-infrastructure/07-01-WAVE0-LOG.md` returns `1`
    - `grep -c "Plan 07-03 instruction:" .planning/phases/07-market-data-api-infrastructure/07-01-WAVE0-LOG.md` returns `1`
    - The log file's `Polygon tier:` line contains exactly one of: `full`, `forbidden`, `endpoint_unknown`
  </acceptance_criteria>
  <resume-signal>Type "wave-0-verified" once the log file exists with all three grep checks passing. Include the Polygon tier outcome (full / forbidden / endpoint_unknown) in the resume message so plan 07-03 can be executed with the correct instruction.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → process | `.env.local` values become `process.env.*` at server boot. Never crosses into client bundle unless prefixed `NEXT_PUBLIC_`. |
| developer machine → Vercel | These env vars must also be configured in Vercel dashboard for production — not handled in this plan, but flagged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY in `.env.local` | mitigate | Acceptance criteria explicitly grep for `NEXT_PUBLIC_` prefix and require absence (count=0). `.env.local` confirmed git-ignored via `git check-ignore`. |
| T-07-03 | Information Disclosure | FRED_API_KEY in `.env.local` | mitigate | Same gate: no NEXT_PUBLIC_ prefix, file git-ignored. FRED key in URL query param is acceptable for free tier (no PII leverage), but cache key array (set in plan 07-03) MUST NOT include the key. |
| T-07-W0-A | Spoofing | Polygon API key used in verification curl | accept | Curl is run on developer machine only; key never leaves local shell. Output written to log file does NOT include the key. |
| T-07-W0-B | Information Disclosure | Wave 0 log file accidentally committing API keys | mitigate | Log template above contains NO key values — only HTTP statuses and tier outcomes. Reviewer rejects PRs containing key values in log file. |
</threat_model>

<verification>
- Both env vars present in `.env.local`, neither with `NEXT_PUBLIC_` prefix
- `.env.local` git-ignored
- Wave 0 log file exists with Polygon tier + AV reachability outcomes
- Plan 07-03 has a clear "build real fetcher" / "build graceful stub" / "drop tool" instruction in the log
</verification>

<success_criteria>
- All four `grep` checks in Task 1 acceptance criteria pass
- Task 2 log file exists with all three required grep markers
- Resume signal received with Polygon tier outcome announced (so 07-03 executor knows which code path to ship)
</success_criteria>

<output>
After completion, create `.planning/phases/07-market-data-api-infrastructure/07-01-SUMMARY.md` recording:
- Polygon tier outcome (full / forbidden / endpoint_unknown)
- AV MCP reachability (yes / no)
- Env vars confirmed in .env.local
- Any anomalies the developer encountered (e.g. AV signup delay, Polygon dashboard navigation issue)
</output>
